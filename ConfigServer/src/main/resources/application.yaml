server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wyaldalex/dummy-spring-boot-configs.git
          clone-on-start: true
